{# Home template #}
{% extends "base.html" %}
{% load static %}

{% block appTitle %}Strona główna{% endblock %}
{% block moduleTitle %}Witamy w RavaDev{% endblock %}
{% block appHeader %}Strona główna{% endblock %}
{% block moduleHeader %}Witamy w RavaDev{% endblock %}

{% block content %}
<!-- Hero Section -->
<section class="" aria-labelledby="hero-heading">
    <div class="hero-content hero-section glass">
        <h1 id="hero-heading" class="hero-title">
            RavaDev — Technologia bliżej ludzi
        </h1>
        <p class="hero-description">
            Profesjonalny outsourcing IT, tworzenie oprogramowania oraz kompleksowe wsparcie technologiczne dla Twojej firmy.
        </p>
        <div class="hero-buttons">
            <a href="{% url 'Contact:contact' %}" class="btn btn-primary" aria-label="Zamów usługę">
                Poproś o bezpłatną wycenę
            </a>
            <a href="{% url 'Services:services' %}" class="btn btn-secondary" aria-label="Poznaj usługi">
                Poznaj usługi
            </a>
        </div>
        <p class="hero-features">
            Bezpłatna wycena • Wdrożenia lokalne i chmurowe • Wsparcie 24/7
        </p>
    </div>
</section>

<!-- CTA Slider -->
<section id="cta" class="cta-section" aria-label="Galeria projektów">
    <div class="cta-slider">
        {% for img in cta_images %}
        <div class="cta-slide {% if forloop.first %}active{% endif %}">
            <img src="_media/{{ img }}" alt="Slajd {{ forloop.counter }} - Przykład realizacji RavaDev">
        </div>
        {% endfor %}
        
        <button class="cta-prev" aria-label="Poprzedni slajd">&#10094;</button>
        <button class="cta-next" aria-label="Następny slajd">&#10095;</button>
        
        <div class="cta-dots">
            {% for img in cta_images %}
            <span class="cta-dot {% if forloop.first %}active{% endif %}" 
                  data-slide="{{ forloop.counter0 }}" 
                  aria-label="Przejdź do slajdu {{ forloop.counter }}">
            </span>
            {% endfor %}
        </div>
    </div>
</section>

<!-- About Section -->
<section id="o-nas" class="about-section section-card" aria-labelledby="about-heading">
    <div class="container glass">
        <h2 id="about-heading">O nas</h2>
        <p>RavaDev to młoda, innowacyjna firma technologiczna z siedzibą w Harkabuzie (gmina Raba Wyżna). Specjalizujemy się w tworzeniu nowoczesnych aplikacji wspierających codzienne życie i biznes. Naszą misją jest dostarczanie prostych, intuicyjnych i bezpiecznych rozwiązań cyfrowych, które ułatwiają zarządzanie finansami, czasem, zdrowiem oraz procesami biznesowymi.</p>
    </div>
</section>

<!-- Blog Section -->
<section id="blog" class="blog-section" aria-labelledby="blog-heading">
    <div class="container">
        <h2 id="blog-heading">Blog</h2>
        
        <div class="categories-grid">
            {% for category in categories %}
            <a href="{% url 'Blog:category' category.id %}" class="category-card">
                <article class="glass widget-container">
                    <h3 class="category-title">{{ category.name }}</h3>
                    <div class="category-content">
                        <div class="category-info">
                            <h4>Kategoria bloga</h4>
                            <p class="post-count">Liczba postów: <strong>{{ category.post_set.count }}</strong></p>
                            <p class="category-description">{{ category.description|truncatechars:256 }}</p>
                        </div>
                    </div>
                </article>
            </a>
            {% endfor %}
        </div>
    </div>
</section>

<!-- Services Section -->
<section id="uslugi" class="services-section" aria-labelledby="services-heading">
    <div class="container">
        <h2 id="services-heading">Usługi</h2>
        
        <div class="services-container">
            {% for field in fields %}
            <div class="field-group glass">
                <h3 class="field-title">{{ field.name }}</h3>
                
                <div class="services-grid">
                    {% for service in services %}
                    {% if service.field == field %}
                    <a href="{% url 'Services:service_detail' service.id %}" class="service-card">
                        <article class="glass widget-container">
                            <h4 class="service-title">{{ service.name }}</h4>
                            
                            <div class="service-content">
                                <div class="service-image">
                                    <img src="{{ service.icon.url }}" 
                                         alt="Ikona {{ service.name }}" 
                                         class="service-icon">
                                </div>
                                
                                <div class="service-info">
                                    <h5 class="service-field">{{ service.field.name }}</h5>
                                    <p class="service-price">Cena: <strong>{{ service.price }}</strong></p>
                                    <p class="service-duration">Czas realizacji: <strong>{{ service.duration }}</strong></p>
                                    <p class="service-description">{{ service.description|truncatechars:256 }}</p>
                                </div>
                            </div>
                        </article>
                    </a>
                    {% endif %}
                    {% endfor %}
                </div>
            </div>
            {% endfor %}
        </div>
    </div>
</section>

<!-- Portfolio Section -->
<section id="realizacje" class="portfolio-section" aria-labelledby="portfolio-heading">
    <div class="container">
        <h2 id="portfolio-heading">Realizacje</h2>
        
        <div class="projects-grid">
            {% for project in projects %}
            <a href="{% url 'Projects:project_detail' project.id %}" class="project-card">
                <article class="glass widget-container">
                    <h3 class="project-title">{{ project.title }}</h3>
                    
                    <div class="project-content">
                        <div class="project-image">
                            <img src="{{ project.main_image.url }}" 
                                 alt="Zdjęcie projektu {{ project.title }}" 
                                 class="project-thumbnail">
                        </div>
                        
                        <div class="project-info">
                            <h4 class="project-client">{{ project.client }}</h4>
                            <p class="project-category">Kategoria: <strong>{{ project.category }}</strong></p>
                            <p class="project-date">Data realizacji: <strong>{{ project.completion_date|date:"d.m.Y" }}</strong></p>
                            <p class="project-description">{{ project.description|truncatechars:256 }}</p>
                            <span class="project-link">Zobacz projekt</span>
                        </div>
                    </div>
                </article>
            </a>
            {% endfor %}
        </div>
    </div>
</section>

<!-- Contact Section -->
<section id="kontakt" class="contact-section" aria-labelledby="contact-heading">
    <div class="container">
        <h2 id="contact-heading">Kontakt</h2>
        
        <div class="contact-form-container glass">
            <form method="post" novalidate class="contact-form">
                {% csrf_token %}
                
                <div class="form-group">
                    <label for="{{ form.sender.id_for_label }}">{{ form.sender.label }}:</label>
                    {{ form.sender }}
                    {% if form.sender.errors %}
                    <div class="error-list">
                        {% for error in form.sender.errors %}
                        <span class="error">{{ error }}</span>
                        {% endfor %}
                    </div>
                    {% endif %}
                </div>

                <div class="form-group">
                    <label for="{{ form.email.id_for_label }}">{{ form.email.label }}:</label>
                    {{ form.email }}
                    {% if form.email.errors %}
                    <div class="error-list">
                        {% for error in form.email.errors %}
                        <span class="error">{{ error }}</span>
                        {% endfor %}
                    </div>
                    {% endif %}
                </div>

                <div class="form-row">
                    <div class="form-group">
                        <label for="{{ form.phoneCode.id_for_label }}">Kod kierunkowy:</label>
                        {{ form.phoneCode }}
                        {% if form.phoneCode.errors %}
                        <div class="error-list">
                            {% for error in form.phoneCode.errors %}
                            <span class="error">{{ error }}</span>
                            {% endfor %}
                        </div>
                        {% endif %}
                    </div>

                    <div class="form-group">
                        <label for="{{ form.phoneNumber.id_for_label }}">Numer telefonu:</label>
                        {{ form.phoneNumber }}
                        {% if form.phoneNumber.errors %}
                        <div class="error-list">
                            {% for error in form.phoneNumber.errors %}
                            <span class="error">{{ error }}</span>
                            {% endfor %}
                        </div>
                        {% endif %}
                    </div>
                </div>

                <div class="form-group">
                    <label for="{{ form.content.id_for_label }}">{{ form.content.label }}:</label>
                    {{ form.content }}
                    {% if form.content.errors %}
                    <div class="error-list">
                        {% for error in form.content.errors %}
                        <span class="error">{{ error }}</span>
                        {% endfor %}
                    </div>
                    {% endif %}
                </div>

                <button type="submit" class="submit-btn btn-primary">Wyślij wiadomość</button>
            </form>
        </div>
    </div>
</section>

<style>
    /* Hero Section Styles */
    .hero-section {
        padding: 48px 20px;
        text-align: center;
        background: linear-gradient(180deg, turquoise, magenta);
        color: #000000;
        box-shadow: 0 0 100px rgba(0, 255, 255, 1);
        border-radius: 50px;
        margin-bottom: 24px;
    }

    .hero-content {
        max-width: 1000px;
        margin: 0 auto;
    }

    .hero-title {
        font-size: 2.25rem;
        margin: 0 0 12px;
        line-height: 1.05;
        text-shadow: 0 0 8px rgba(0, 0, 0, 0.25);
    }

    .hero-description {
        font-size: 1.125rem;
        margin: 0 0 24px;
        opacity: 0.9;
        color: #333;
    }

    .hero-buttons {
        display: flex;
        gap: 12px;
        justify-content: center;
        flex-wrap: wrap;
        margin-bottom: 18px;
    }

    .btn {
        display: inline-block;
        padding: 14px 22px;
        text-decoration: none;
        border-radius: 10px;
        font-weight: 600;
        transition: all 0.3s ease;
    }

    .btn-primary {
        background: #06b6d4;
        color: #042d35;
    }

    .btn-primary:hover {
        background: #0891b2;
        transform: translateY(-2px);
    }

    .btn-secondary {
        background: transparent;
        color: #e6f7fb;
        border: 2px solid rgba(230, 247, 251, 0.25);
    }

    .btn-secondary:hover {
        background: rgba(230, 247, 251, 0.1);
        border-color: rgba(230, 247, 251, 0.5);
    }

    .hero-features {
        margin-top: 18px;
        font-size: 0.95rem;
        opacity: 0.85;
        color: #555;
    }

    /* Section Cards */
    .section-card {
        margin-bottom: 2rem;
    }

    /* Grid Layouts */
    .categories-grid,
    .services-grid,
    .projects-grid {
        display: grid;
        gap: 1.5rem;
        grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    }

    /* Card Styles */
    .category-card,
    .service-card,
    .project-card {
        text-decoration: none;
        color: inherit;
        transition: transform 0.3s ease;
    }

    .category-card:hover,
    .service-card:hover,
    .project-card:hover {
        transform: translateY(-5px);
    }

    .widget-container {
        padding: 1.5rem;
        margin-bottom: 1rem;
    }

    /* Content Layouts */
    .service-content,
    .project-content {
        display: flex;
        gap: 1rem;
        align-items: flex-start;
    }

    .service-image,
    .project-image {
        flex: 0 0 100px;
    }

    .service-icon,
    .project-thumbnail {
        max-width: 100%;
        height: auto;
        border-radius: 4px;
    }

    .service-info,
    .project-info {
        flex: 1;
    }

    /* Form Styles */
    .form-row {
        display: grid;
        grid-template-columns: 1fr 2fr;
        gap: 1rem;
    }

    .contact-form {
        max-width: 600px;
        margin: 0 auto;
    }

    .form-group {
        margin-bottom: 1rem;
    }

    .form-group label {
        display: block;
        margin-bottom: 0.5rem;
        font-weight: 600;
    }


    .error-list {
        color: #dc2626;
        font-size: 0.875rem;
        margin-top: 0.25rem;
    }

    /* Responsive Design */
    @media (max-width: 768px) {
        .hero-title {
            font-size: 1.75rem;
        }

        .hero-buttons {
            flex-direction: column;
            align-items: center;
        }

        .btn {
            width: 100%;
            max-width: 300px;
        }

        .service-content,
        .project-content {
            flex-direction: column;
        }

        .service-image,
        .project-image {
            flex: none;
            width: 100%;
        }

        .form-row {
            grid-template-columns: 1fr;
        }
    }
</style>
{% endblock %}

{% block extra_js %}
<script>
    document.addEventListener("DOMContentLoaded", function() {
        // CTA Slider functionality
        const slides = document.querySelectorAll(".cta-slide");
        const dots = document.querySelectorAll(".cta-dot");
        let currentIndex = 0;
        let timer;

        function showSlide(index) {
            slides.forEach((s, i) => {
                s.classList.remove("active");
                dots[i]?.classList.remove("active");
            });
            
            if (slides[index]) {
                slides[index].classList.add("active");
                dots[index]?.classList.add("active");
                currentIndex = index;
            }
        }

        function nextSlide() {
            let newIndex = (currentIndex + 1) % slides.length;
            showSlide(newIndex);
        }

        function prevSlide() {
            let newIndex = (currentIndex - 1 + slides.length) % slides.length;
            showSlide(newIndex);
        }

        function startAutoSlide() {
            timer = setInterval(nextSlide, 4000);
        }

        function stopAutoSlide() {
            clearInterval(timer);
        }

        // Event listeners for slider controls
        const nextButton = document.querySelector(".cta-next");
        const prevButton = document.querySelector(".cta-prev");

        if (nextButton) {
            nextButton.addEventListener("click", () => {
                stopAutoSlide();
                nextSlide();
                startAutoSlide();
            });
        }

        if (prevButton) {
            prevButton.addEventListener("click", () => {
                stopAutoSlide();
                prevSlide();
                startAutoSlide();
            });
        }

        // Dot navigation
        dots.forEach(dot => {
            dot.addEventListener("click", (e) => {
                stopAutoSlide();
                showSlide(parseInt(e.target.dataset.slide));
                startAutoSlide();
            });
        });

        // Initialize slider
        if (slides.length > 0) {
            showSlide(currentIndex);
            startAutoSlide();
        }

        // Smooth scrolling for anchor links
        document.querySelectorAll('a[href^="#"]').forEach(anchor => {
            anchor.addEventListener('click', function (e) {
                e.preventDefault();
                const target = document.querySelector(this.getAttribute('href'));
                if (target) {
                    target.scrollIntoView({
                        behavior: 'smooth',
                        block: 'start'
                    });
                }
            });
        });
    });
</script>
{% endblock %}